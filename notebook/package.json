{ 
  "scripts": {
  "frontend:install": "cd notes-frontend && npm install ",
  "frontend:start": "cd notes-frontend && npm run start",
  "frontend:deploy": "cd notes-frontend && npm run deploy",

  "api:install": "cd notes-api && npm install .",
  "api:start": "cd notes-api && npm start .",
  "api:deploy": "cd notes-api && npm run deploy",

  "deploy": "npm run api:install && npm run frontend:install &&  npm run api:deploy && npm run frontend:deploy"
}
}
